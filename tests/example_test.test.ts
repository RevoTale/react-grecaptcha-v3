describe('someTestExampleName', () => {
    it('Should do some test of library', () => {

        const expectedResult = 'good value'
        expect(expectedResult).toEqual('good value');
    });
});
